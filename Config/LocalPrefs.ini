// The following are settable by the user. They are assigned to the
// default values, as if this file was empty. Comments above them
// should indicate what possible settings are available.

// Note that only things that cannot affect game synchronization are
// in here (views, etc).

// User's changes should be placed in this file, below the
// [UserPrefs] line below.

[UserPrefs]
TeamColorTeamA = "255 255 255 255" // white
TeamColorTeamB = " 31  31  31 255" // black

MainThreadAffinityMask = 1
NetworkThreadAffinityMask = 2
OggDecodeThreadAffinityMask = 4

// Default name to be in the session name box 
DefaultSessionName = "BZCC Reimagined Session"

// Default server message
DefaultMessage = "Mod created by AI_Unit. Available on the Steam Workshop."