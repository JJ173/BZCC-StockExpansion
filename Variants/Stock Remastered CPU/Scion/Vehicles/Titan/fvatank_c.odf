[GameObjectClass]
classLabel = "fvatank_x"

scrapValue = 30 // Scrap value for the CPU is reduced by half so it drops less loose.
scrapCost = 45 // Scrap cost is reduced by 50%.

[CraftClass]
CanAIPForceIdle = false // Added to try and prevent the AIP from forcing the unit to an idle state when built.

[TrackedVehicleClass]

[AssaultTankClass]